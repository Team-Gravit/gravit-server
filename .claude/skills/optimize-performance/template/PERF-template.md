# [PERF-{이슈번호}] {대상 엔드포인트}

> 이슈: #{이슈번호}
> 브랜치: {브랜치명}

## 진행 상태

> ⏳ 미완 / ✅ 완료 / ⏭️ 건너뜀
> 재진입 시 ⏳로 표기된 가장 이른 Phase부터 재개한다.

**준비 (대상당 1회)**

| 1. 대상 | 2. 환경 | 3. 조건 | 4. 기준선 |
|---|---|---|---|
| ⏳ | ⏳ | ⏳ | ⏳ |

**사이클 (반복)**

| # | 기법 | 5. 설계 | 6. 스냅샷 | 7. 적용 | 8. 검증 |
|---|---|---|---|---|---|
| 1 | | ⏳ | ⏳ | ⏳ | ⏳ |

## 대상

- 엔드포인트: `{HTTP} {경로}`
- 실행 경로: `{Controller}` → `{Facade}` → `{Service}` → `{Repository}`
- 예상 쿼리 목록 (요청 1회 기준)
  1. `{Repository.메서드}` - {쿼리 요지}
  2. {지연 로딩 참조 지점이 있으면 추가 쿼리 후보로 표시}

## 측정 환경

> 값이 바뀌면 그 사실을 사이클 기록에 남긴다.

| 항목 | 값 |
|---|---|
| 프로파일 | perf |
| 커넥션 풀 크기 | {maximum-pool-size} |
| 데이터 규모 | {대상 테이블별 행 수} |
| 카디널리티 | {컬럼별 서로 다른 값의 개수} |
| 부하 조건 | VU {n}, duration {t} |
| 캐시 상태 | cold (measure 직전 FLUSHALL) / warm |
| 캐시 제어 수단 | `redis-cli` / `docker exec gravit-redis-local redis-cli` |
| 시드 SQL | `seeds.sql` / 미사용 |

## 기준선 (Baseline)

| 지표 | 값 |
|---|---|
| p95 | |
| p99 | |
| RPS | |
| 에러율 | |
| check 통과율 | |
| 요청당 쿼리 수 | |

### 쿼리 통계 (total_exec_time 상위)

> 원본: `query-stats-0.txt` / k6 요약 원본: `k6-test-summary-0.json`
> 여기에는 진단 근거로 쓴 행만 옮긴다. 전체를 복사하지 않는다.

| calls | mean_ms | total_ms | query |
|---|---|---|---|

### 진단

- 병목 성격: {Phase 4 진단 표의 판정}
- 근거: {관측된 수치}

---

## 사이클 {n}: {적용한 기법}

### 설계 결정

> Phase 5-B에서 호출자와 확정한 내용.

| 결정 항목 | 정한 값 | 근거 |
|---|---|---|

- 검토했지만 택하지 않은 안: {안} - {배제 근거}
- 호출자가 예상한 효과: {지표와 방향, 예: p95 30% 감소 / Seq Scan → Index Scan}

### 개선 전 지표

| 지표 | 값 |
|---|---|
| p95 / p99 | |
| RPS | |
| 요청당 쿼리 수 | |
| 대상 쿼리 calls / mean_ms / total_ms | |

**실행계획**

> 원본: `query-plan-{n-1}.txt` (개선 후는 `query-plan-{n}.txt`)

- EXPLAIN 파라미터: {$1 = 값, $2 = 값} (이후 모든 사이클에서 동일하게 사용)
- 스캔 방식: {Seq Scan / Index Scan / ...}, 사용 인덱스: {인덱스명}
- actual rows 대 반환 행 수: {n} / {m}
- Rows Removed by Filter: {n}
- shared hit / read: {n} / {m}
- 플래너 추정 대 실측: {rows=n} / {actual rows=m}

{기법별 추가 캡처 - 캐싱이면 반복 호출 비율과 무효화 경로, 로직이면 호출 스택, 풀이면 획득 대기 시간}

### 적용 내용

- {수정한 파일과 변경 요지}
- 테스트: {`./gradlew test` 결과}

### 개선 후 지표

| 구분 | 지표 | 개선 전 | 개선 후 | 변화 |
|---|---|---|---|---|
| 하드웨어 의존 | p95 | | | |
| | p99 | | | |
| | RPS | | | |
| 하드웨어 독립 | 요청당 쿼리 수 | | | |
| | 대상 쿼리 total_ms | | | |
| | 검사 행 / 반환 행 | | | |
| | 스캔 방식 | | | |

### 판정

- 개선 여부 (하드웨어 독립 증거 기준): {있음 / 없음 - 근거}
- 남은 위험 신호: {Phase 6 표에서 아직 해소되지 않은 항목}
- 다음 사이클 진행 여부: {호출자 결정}

---

## 최종 요약

> 하드웨어 의존 증거와 독립 증거를 모두 남긴다.

| 구분 | 지표 | 최초 | 최종 | 변화 |
|---|---|---|---|---|
| 하드웨어 의존 | p95 | | | |
| | p99 | | | |
| | RPS | | | |
| 하드웨어 독립 | 요청당 쿼리 수 | | | |
| | 검사 행 / 반환 행 | | | |
| | 스캔 방식 | | | |

적용한 기법: {사이클 순서대로}
