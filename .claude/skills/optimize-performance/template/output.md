<!--
작성 가이드 (이 주석은 출력에 포함하지 마라):
- Phase 9는 사이클이 끝났을 때만 실행된다. 여기에 사이클 중간 보고를 쓰지 마라.
- 아래 항목만 채우고 그 외 서술을 덧붙이지 마라.
- 수치는 record.md의 **최종 요약**에 저장된 값만 쓴다. 받지 못한 값을 추정해서 채우지 마라.
- 최초는 `-0` 상태, 최종은 마지막 사이클 번호의 상태다.
- 적용한 기법은 사이클 순서대로 나열한다. 개선 없이 끝난 사이클도 빼지 말고 적는다.
-->
## 성능 최적화 완료 - `{HTTP} {경로}`

- **사이클**: {n}회
- **적용한 기법**: {1. 기법 → 2. 기법 → ...}
- **하드웨어 독립**: 요청당 쿼리 {최초} → {최종}, 검사 행/반환 행 {최초} → {최종}, 스캔 {최초} → {최종}
- **하드웨어 의존**: p95 {최초} → {최종}, p99 {최초} → {최종}, RPS {최초} → {최종}
- **종료 사유**: {Phase 8 판정 표의 조건}
- **기록**: `.claude/resources/perf/{이슈번호}/record.md`
- **다음**: `commit-push` → `open-pr`
