<!--
작성 가이드 (이 주석은 출력에 포함하지 마라):
- 아래 항목만 채우고 그 외 서술을 덧붙이지 마라.
- base가 dev가 아니면(main 폴백 등) base 값에 그 사실이 드러나게 적어라.
-->
## 발의 완료

- **이슈**: [#{번호}]({URL}) · {종류}
- **브랜치**: `{브랜치명}` (base: {base})
- **다음**: `write-plan`
