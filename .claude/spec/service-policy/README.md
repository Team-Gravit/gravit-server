---
description: 서비스, 비즈니스 관점의 도메인 정책 단일 출처(SSOT). 작성 규칙과 도메인별 파일 목록
---

# Service Policy

Gravit 서비스 정책의 단일 출처다. 정책을 확인할 때 코드를 읽지 않아도 되게 하는 것이 목적이다.
도메인별로 파일을 나눠, 필요한 도메인만 읽고 끝낼 수 있게 한다.

## 작성 규칙

- **담는 것**: 사용자에게 드러나는 규칙과 판정 기준. 지급과 미지급 조건, 성공과 실패 조건, 산정 기준, 주기와 초기화 시점, 권한 범위
- **담지 않는 것**: 클래스 구조, 파일 경로, 메서드 시그니처, 쿼리, 트랜잭션 처리 방식
- 위치 표기가 꼭 필요하면 `LearningFacade:81` 수준의 클래스명과 라인까지만 쓴다. 파일 경로는 쓰지 않는다
- 문장은 "무엇을 하면 무엇이 된다" 형태로 쓴다
- 정책이 없는 도메인도 파일을 지우지 말고 `- 별도 정책 없음`으로 남긴다. 지우면 누락과 구분되지 않는다
- 다른 도메인에 이미 적힌 정책을 옮겨 쓰지 마라. `{파일명}을 따른다`로 넘겨라
- 문서와 코드가 어긋나면 이 문서를 기준으로 삼고, 어긋난 사실을 사용자에게 보고한다

## 갱신 규칙

코드 변경으로 위 "담는 것"에 해당하는 규칙이 달라지면 **같은 작업에서** 해당 도메인 파일을 함께 고친다.
`write-plan`은 계획 단계에서 대상 도메인 파일을 읽고, `implement`는 구현 후 바뀐 정책을 반영한다.
도메인이 새로 생기면 파일을 추가하고 아래 목록에 한 줄 넣는다.

## 도메인별 파일

| 파일 | 범위 |
|------|------|
| `auth-security.md` | 소셜 로그인, 토큰, 권한 승격 |
| `user.md` | 레벨과 XP, 온보딩, 프로필, 탈퇴와 복구 |
| `friend-social.md` | 팔로우, 소셜 피드, 축하 |
| `content.md` | 챕터, 유닛, 레슨, 문제, 선택지의 구조와 제출 규칙 |
| `learning.md` | 학습 보상, 연속 학습일, 일일과 주간 기록 |
| `bookmark-note.md` | 북마크, 오답 노트 |
| `cs-note.md` | CS 노트 제공 방식 |
| `league-season.md` | 리그 티어, 리그 점수, 시즌 롤오버 |
| `mission.md` | 일일 미션 배정, 진행, 완료 보상 |
| `notification.md` | 인앱 알림과 푸시 발송 조건, 스케줄 |
| `notice.md` | 공지 |
| `inquiry.md` | 문의와 답변 |
| `admin-report.md` | 어드민 권한, 콘텐츠 승격, 신고 |
| `version.md` | 앱 버전 |
