services:
  gravit-server:
    container_name: gravit-server
    image: ${DOCKER_HUB_USERNAME}/gravit-server:latest
    ports:
      - "8520:8080"
    environment:
      - TZ=Asia/Seoul
    networks:
      - mogaktae_js-bridge

networks:
  mogaktae_js-bridge:
    external: true